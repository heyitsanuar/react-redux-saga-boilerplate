import React from 'react';

export interface IProgramsProps {
 
}

export const ProgramsContainer = (props: IProgramsProps) => {
  const { } = props;

  return (
        <div>
            Full Redux Container
        </div>
  );
};
